require_relative "../spec_helper"

describe Gemcase::Gemstash do
  context "default installation" do
    xit "serves the homepage instead of redirecting to rubygems.org" do

    end

    xit "uses the custom header and footer not provided by Gemstash" do

    end

    xit "displays the contact information in the webpage" do

    end

    xit "caches the private gems index view" do

    end

    xit "welcomes the authenticated user" do

    end
  end
end